# Environments
.venv/
__pycache__/
*.pyc

# Data (gitignored per brief)
data/raw/
data/processed/*
# Exception: committed so the full pipeline is runnable without redoing
# the seed-labeling visual-review step (see RUN.md).
!data/processed/mood_labels_seed.csv

# Trained artifacts (gitignored per brief)
artifacts/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.claude/

# Secrets
.env
kaggle.json
