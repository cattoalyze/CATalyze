# Environments
.venv/
__pycache__/
*.pyc

# Data (gitignored per brief)
data/raw/
data/processed/*
# Exception: committed so the full pipeline is runnable without redoing
# the seed-labeling visual-review step.
!data/processed/mood_labels_seed.csv

# Trained artifacts (gitignored per brief)
artifacts/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.claude/

# run.sh/stop.sh runtime state (PID file, server log)
.run/

# Secrets
.env
kaggle.json

# Only README.md is published; other planning/reference docs stay local
*.md
!README.md
